# OpenClaw 本地 AI 环境 + 控制台 v2.2

> 最后更新：2026-09-13

## 这是什么

一套**纯本地**的 AI 环境，重装系统也能一键恢复：

- **llama 模型服务**（端口 8080）：本地大模型，负责对话推理
- **ComfyUI 生图服务**（端口 8189）：本地出图
- **OpenClaw 网关**（端口 18789）：微信 / 网页通道，聊天窗口直接对话和生图
- **OpenClaw 控制台 v2.2**：管理上面三个服务的图形界面

## 目录结构

```
L:\OpenClaw\
├── OpenClaw控制台_v2.2.exe      ← 主程序（双击运行）
├── 一键恢复.bat                  ← 重装系统后双击，自动拉起全部服务
├── llama\                        ← 模型 + llama-server（约 78GB）
│   └── models\gemma-4-12b-it-Q4_0.gguf 等
├── ComfyUI\                      ← 纯净版 ComfyUI（约 61GB）
├── OpenClawData\                 ← 配置 / 控制台源码 / 日志 / 微信账号
│   ├── openclaw.json             ← OpenClaw 配置（MCP、模型、通道）
│   ├── gateway.cmd               ← 网关启动器（便携 node 优先）
│   └── console\
│       ├── openclaw_console.py   ← 控制台源码（改功能改这里）
│       ├── comfyui_mcp_server.py ← 生图 MCP 服务
│       ├── restore_env.py        ← 一键恢复核心逻辑
│       └── logs\                 ← llama / gateway / comfyui 日志
├── python\                       ← 便携 Python（MCP 执行器，已装 PyInstaller）
├── node\                         ← 便携 Node（重装系统后不用装 node）
├── npm\                          ← openclaw 便携副本
├── backup_old_openclaw\          ← C 盘旧配置备份（848MB）
└── _trash_cleanup\               ← 过期版本暂存（确认后删）
```

## 控制台 v2.2 功能

- 模型 / ComfyUI / 网关 启停控制 + 状态灯
- **生图默认模型**下拉（选即保存，MCP 生图自动用）
- **生图默认 LoRA** 两个下拉（Krea2 / Z-Image 独立配置）
- 清理内存 / 清理显存 按钮（调 ComfyUI 清理节点）
- **上传文件夹 / 生成文件夹** 打开按钮（方便清理）
- **硬件配置**：检测 GPU/显存/内存 → 推荐 llama 参数 → 一键应用
- **一键恢复**：检查并拉起缺失服务
- **开机自启**：模型 / 网关 / ComfyUI 三个勾选
- 日志窗口（磁吸可调）

## 重装系统后恢复（三步）

1. 安装 Node.js 24+（或直接用 `L:\OpenClaw\node` 便携版，无需安装）
2. 双击 `L:\OpenClaw\一键恢复.bat`
3. 打开 `L:\OpenClaw\OpenClaw控制台_v2.2.exe` 勾选三个开机自启

> 原理：所有软件都在 L 盘，`OPENCLAW_STATE_DIR` 环境变量指向 `L:\OpenClaw\OpenClawData`，
> C 盘只需 node（L 盘 node 便携版可替代），微信账号无需重新登录。

## 版本记录

| 版本 | 说明 |
|---|---|
| v2.2 | 硬件配置推荐 + 一键恢复按钮 + ComfyUI 开机自启 + 下拉框加宽 + 路径全量 L:\OpenClaw |
| v2.1 | LoRA 双下拉 + 上传/生成文件夹按钮 + C 盘残留路径清理 |
| v2.0 | 迁移到 L 盘根目录前版本 |

## 常用命令

- 启动全部：双击 `一键恢复.bat`
- 生图测试：`L:\OpenClaw\python\python.exe test_mcp_gen_new.py`（结果看 gen_new.txt）
- 打包新版本：`L:\OpenClaw\python\python.exe -m PyInstaller --noconfirm --distpath L:\OpenClaw OpenClaw控制台_v2.2.spec`
- 日志：`L:\OpenClaw\OpenClawData\console\logs\`
